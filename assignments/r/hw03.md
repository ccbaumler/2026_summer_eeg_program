---
layout: page
title: Learning and Plotting
permalink: /assignments/r/hw03
nav_order: 4
parent: R Assignments
grand_parent: Assignments
---

## Learning code on your own and Plotting what you know **Due Friday, Jul 17th at 12pm PST**{: .label .label-red }

### Use a Vignette

As shown during our lecture period, the R ecosystem contains various quality vignettes (tutorials) built-in to many packages. These vignettes are authored by the same people who wrote the packages containing the vignettes; they can be a very quick way to learn the package you are going to use in an R script or R markdown notebook.

For part 1 of this week's homework:

1. Find a vignette for an R library you wish to learn to use.
2. Introduce the vignette and explain its importance to your research.
    1. Be sure to include the link to the vignette you are mimicing.
3. Follow along with the vignette in a separate R markdown. The notebook should contain:
    1. Echo'd code
    2. Output from the code
    3. Basic Markdown formatting
    4. The file name should be your `first_name_vignette.rmd`
4. Explain what each section does in your own words.
5. Knit and email the html file to me.

{: .note}
> Make sure to keep this code chunk at the top of your notebook:
>
> ```r
> knitr::opts_chunk$set(echo = TRUE)
> ```

### Holistic approach to data

Visualizing our data properly can be vital in communicating complex ideas to a wide audience with a variety expertise. Learning the steps to explore a dataset in R is a valuable skill that *can* quickly give insight into the data you are using.

Complete the various tasks below in an `RMarkdown` document named `first_name_plotting.rmd`. **To submit, email me the html file from knitting**

#### Part 1: Meet Your Data (Metadata)

1. Pick a Dataset

    Choose one of the built-in datasets in R (like `mpg` for cars or `diamonds` for jewelry). See Homework 1 for the complete list of base r datasets.

    - Dataset Name: __________

2. Data Biography (Background)

    In 2–3 sentences, answer:

    - Who collected the data?

    - Where did this data come from?

    - What is it measuring?

    - Why was this data collected?

    - How were the measurements recorded?

3. Data Dictionary (The Columns)

    Pick three columns (variables) from your dataset that you want to look at; one must be numerical and one must be strings. What do they mean?

    - Column 1: [Name] = What it means: __________

    - Column 2: [Name] = What it means: __________

    - Column 3: [Name] = What it means: __________

#### Part 2: Get to know your Data (Data Forensics)

1. Use simple commands like `summary()` or `str()` or `dplyr::glimpse()` to answer these two questions:

    - How many rows (observations) are in your dataset? __________

    - How many columns (variables) are there? __________

2. Use simple commands like `mean()` and `max()` to find the average and max of a variable:

    - What is the average value? __________

    - What is the maximum value? __________

3. Use simple commands like `table()` to find the count of observations for a variable:

    - Which observation appears the most? __________

    - Which observation appears the least? __________

#### Part 3: Plot the Data

Now, use `ggplot2` to make a graph to visualize some story the data hides.

1. Write Your Code

    {: .note}
    > Replace "dataset_name", "column_x", and "column_y" with your data
    >
    > ```r
    > ggplot(data = dataset_name, aes(x = column_x, y = column_y)) + 
    >   geom_point() +  # Makes a scatter plot. Pick what makes sense for your data!
    >   labs(title = "My First R Plot", x = "X-Axis Label", y = "Y-Axis Label")
    > ```

2. Explain Your Graph

    - What does this graph show? (e.g., "As X goes up, Y goes down." or "There is no clear pattern.")
